# monthly-build-gallery — account rules

This repo belongs to the **matthewignash-unified** GitHub account (work account).

- Before ANY `git push` or `gh` command: run `gh auth switch --user matthewignash-unified`,
  then confirm with `gh auth status`.
- Commits here use local git config only: user.name "Matthew Ignash",
  user.email "matthewignash.unified@gmail.com". Never touch global git config.
- Never create or push repos for this project under the personal account
  (`matthewignash`).
